{
  "name": "ai-workflow-hub",
  "version": "0.1.0",
  "description": "Desktop app for managing and launching AI workflow sessions",
  "type": "module",
  "scripts": {
    "start": "node scripts/main.js",
    "test": "node --test tests/",
    "lint": "eslint scripts/"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9.0"
  }
}

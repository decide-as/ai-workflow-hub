# What's Next Discovery

When the user wants to discover what to work on next, or asks "what should I build?", invoke the `/next` skill. It runs 7 analyzers against project state and ecosystem best practices, auto-scores each candidate via initiative scoring, and presents a ranked table.

Do not manually analyze the project. Use `/next`.

# Coding Practices

When starting a new feature, module, or significant code change, invoke the `/practices` skill to review applicable coding standards. It loads only the practices relevant to this project based on `project-meta.yaml` metadata.

Also invoke `/practices` during code review to verify compliance with project standards.

Do not manually recall practices from memory. Use `/practices`.

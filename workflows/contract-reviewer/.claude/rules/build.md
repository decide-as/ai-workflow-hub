# World-Class Feature Build Pipeline

When starting a new feature that has a PRD, invoke the `/build` skill. It orchestrates the disciplined process of deep code reading, test-first thinking, incremental verified implementation, and conceptual self-review that produces world-class output.

Do not trigger for bug fixes, single-file changes, test additions for existing code, or when the user says to skip.

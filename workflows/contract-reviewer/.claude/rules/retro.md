# Retrospectives

When the user wants a retrospective or asks to reflect on recent work, invoke the `/retro` skill. It runs data-driven analysis across 7 dimensions (delivery, quality, velocity, debt, compliance, alignment, evolution) and produces an actionable report with trend tracking.

Do not manually analyze git history. Use `/retro`.

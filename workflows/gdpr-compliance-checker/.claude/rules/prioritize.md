# Initiative Prioritization

When the user wants to evaluate, score, or rank an initiative or feature, invoke the `/prioritize` skill. It implements the initiative scoring framework — a multi-dimensional scoring system with 35 dimensions across Value, Risk, Constraints, and Energy, with profile-based weight tuning for different project contexts (enterprise, small team, solo dev, open source).

Do not manually score or rank initiatives. Use `/prioritize`.

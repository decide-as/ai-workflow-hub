# Copy this file to .env in the repo root and fill in your key.
# Loaded automatically in dev by the Electron main process.
# In worktrees, .env is read from the main repo root — no per-worktree file needed.
OPENAI_API_KEY=sk-...
